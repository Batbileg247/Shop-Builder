/**
 * @deprecated Import from `@/context/DashboardContext` instead.
 * Re-exported so older relative imports keep resolving to the same provider.
 */
export {
  DashboardProvider,
  useDashboard,
} from "./DashboardContext";
export type {
  DashboardDataSource,
  DashboardRemoteStatus,
} from "./DashboardContext";
