# Database reference

`reference-schema.sql` (raw SQL) and `schema.ts` (Drizzle `pgTable`) describe the same tables. Copy either into your API service.

The Next.js app does not run migrations from this folder.
