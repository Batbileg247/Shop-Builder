{
  "name": "shop-builder",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix user",
    "build": "npm run build --prefix user",
    "start": "npm run start --prefix user",
    "lint": "npm run lint --prefix user"
  }
}
