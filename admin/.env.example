# Public API bases (see backend-book.md).
# Хуулбар: cp .env.example .env.local — өөрийн URL, ADMIN_SECRET энд.
# .env.local байхгүй ч болно: admin/next.config.ts дээрх жишээ Workers суурь ашиглагдана.
NEXT_PUBLIC_PLATFORM_API_URL=https://unlimited-team-platform-api.gankhulug12345.workers.dev
NEXT_PUBLIC_MERCHANT_API_URL=https://unlimited-team-merchant-api.gankhulug12345.workers.dev

# Server-only: sent as `x-admin-secret` on every platform/merchant client request.
ADMIN_SECRET=
